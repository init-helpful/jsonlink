from setuptools import setup

setup(
    name="jsonlink",
    version="0.0.6",
    description="Allows for a python object to be updated via a dictionary or JSON file.",
    py_modules=["jsonlink"],
    package_dir={"": "src"},
)
